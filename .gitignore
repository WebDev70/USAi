.env
Archive/

# Local Python virtual environment
.venv/

# Editor-local settings (machine-specific, not source)
.vscode/

# Continue extension — MCP server configs may contain API keys (e.g. Context7)
.continue/mcpServers/

# Runtime/local state written by the server (not source)
chat_history.json
.chat_sessions/
.raw_responses/
.chunk_cache/
logs/*.log
logs/*.jsonl
# Project workspaces + uploaded project files created at runtime by
# backend/server.py (PROJECTS_DIR). User data, never source.
.projects/

# Python bytecode caches — build artifacts, never source.
__pycache__/
*.py[cod]

# macOS cruft
.DS_Store

# Dev-only test-coverage artifacts (coverage.py). The .coveragerc config IS
# tracked; its output files are not. Coverage tooling never ships in the app.
.coverage
.coverage.*
htmlcov/
coverage.xml

# Guard against vault files being written to a literal relative path inside the
# repo (e.g. when an Obsidian filesystem-write fallback uses the wrong base path).
# Vault notes belong in the real Obsidian Vault, never tracked here.
Users/
Library/

# Node dev-only dependencies (jsdom for behavior tests). Never shipped in app.
node_modules/

